const initialState = {
  decodeMessage: "",
  encodeMessage: "",
};

const vigenereReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default vigenereReducer;
